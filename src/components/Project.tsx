export interface ProjectProps {
  name: string;
  description: string;
  href?: string;
}

const Project = () => {
  return <div className="bg-gray-50 rounded h-72">Placeholder</div>;
};

export default Project;
